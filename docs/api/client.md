# HomeboxClient

The `HomeboxClient` is the top-level entry point for the py-homebox library.
It manages authentication and exposes all resource-specific sub-clients as attributes.

::: homebox.client.HomeboxClient
